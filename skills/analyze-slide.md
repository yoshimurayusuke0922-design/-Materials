# analyze-slide

## 目的

スライド画像を参考に、タイトル、本文、カード、図解、注釈、背景、サイドレールなどの編集可能な要素へ分解する。

## 入力

- `out/sanko_html/slide_images/{元画像ファイル名}`

## 出力

- `work/analysis/{元画像ファイル名の拡張子なし}.md`

## 手順

1. スライドの役割と主要メッセージを読む。
2. テキストとして起こすべき要素を抽出する。
3. CSS/HTMLで再現する装飾と、画像アセットで扱う要素を分ける。
4. 完全一致より編集性を優先する差分許容点を残す。
