---
name: html-slide-parser
description: Parse an HTML/CSS slide deck into analysis/slide_structure.json and analysis/extracted_assets.json. Use when converting editable HTML proposal slides into structured intermediate data before rebuilding them in Google Slides.
---

# html_slide_parser

Use `tools/html_slide_parser.js` first. Preserve all text as UTF-8 and treat missing images as non-blocking analysis findings.

Read `README.md` in this folder for the exact inputs, outputs, command, sample, and error checks.
