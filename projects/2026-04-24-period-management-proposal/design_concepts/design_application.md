# Design Application Notes

## Direction Taken

- Use `concept_01_cover.png` only as a visual direction for cover polish. The final cover remains editable and keeps the prior deck's red/white identity.
- Use `concept_02_content.png` for dense content slides: red chapter rail, stronger top hierarchy, three-card structure, and a bottom insight/action bar.
- Use `concept_03_mock_ui.png` for mock UI slides: dark left sidebar, top bar, metrics, workflow cards, bottom data area, and right-side explanation cards.

## Final Deck Rule

The proposal generator in `../google_slides_generator/Code.gs` recreates these directions with native Google Slides shapes and text. The image2 PNGs are not pasted into the proposal deck.

## Target Folder

The generator is configured for:

`16MBLby8NutejHMjyz9--MQw6Dk-nWHvI`
